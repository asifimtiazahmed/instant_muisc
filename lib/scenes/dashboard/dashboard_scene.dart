import 'package:flutter/material.dart';

class DashboardScene extends StatelessWidget {
  static const String routeName = 'dashboardScene';

  const DashboardScene({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(),
      ),
    );
  }
}
